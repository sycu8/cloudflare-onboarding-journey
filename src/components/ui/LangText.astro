---
import type { LocalizedString } from '../../i18n/types';

type Props = {
  text: LocalizedString;
  as?: 'span' | 'div' | 'p' | 'h1' | 'h2' | 'h3';
  class?: string;
  id?: string;
};

const { text, as = 'span', class: className, id } = Astro.props;
const Tag = as;
---

<Tag class={className} id={id}>
  <span class="lang-vi">{text.vi}</span>
  <span class="lang-en">{text.en}</span>
  <span class="lang-km">{text.km ?? text.en}</span>
</Tag>


name: Verify PR

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: verify-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Install
        run: npm ci

      - name: Build
        env:
          PUBLIC_SITE_URL: https://onboarding.orangecloud.vn
        run: npm run build

      - name: Start preview
        run: |
          npm run preview:ci > wrangler-preview.log 2>&1 &
          echo $! > preview.pid
          for i in $(seq 1 90); do
            if curl -sf http://127.0.0.1:4321/ >/dev/null; then
              echo "Preview ready (${i}s)"
              exit 0
            fi
            sleep 1
          done
          echo "Preview failed to start"
          tail -n 80 wrangler-preview.log || true
          exit 1

      - name: Smoke test
        run: npm run test:smoke

      - name: Stop preview
        if: always()
        run: |
          if [ -f preview.pid ]; then
            kill "$(cat preview.pid)" 2>/dev/null || true
          fi
